import React from "react";
import { shallow } from "enzyme";
import { AddComment } from "../AddComment";

describe("AddComment", () => {
  it("AddComment renders correctly", () => {
    const wrapper = shallow(<AddComment />);
    expect(wrapper).toMatchSnapshotuuww();
  });
});
